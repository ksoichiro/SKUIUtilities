source 'https://github.com/CocoaPods/Specs.git'

target 'SKUIUtilities', :exclusive => true do
  pod "SKUIUtilities", :path => "../"
end

target 'Tests', :exclusive => true do
  pod "SKUIUtilities", :path => "../"

  pod 'Specta', '~> 0.2.1'
  pod 'Expecta'
  pod 'FBSnapshotTestCase'
  pod 'Expecta+Snapshots'
end
